interface:
  display_name: "Context Handoff"
  short_description: "Safely move long Codex work into a fresh thread"
  default_prompt: "Use $context-handoff to checkpoint this task and continue it in a fresh verified thread."
